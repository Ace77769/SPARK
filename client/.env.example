# For development, Webpack dev server uses the "proxy" field in package.json.
# For production Docker/Render, Nginx proxies /api traffic automatically to the backend container.
# No build-time variables are strictly required.
